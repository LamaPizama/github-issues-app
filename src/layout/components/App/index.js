import styled from 'styled-components'

import background from '@gia/assets/background.jpg'

export default styled.div`
    width: 100%;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background-image: url(${background});
    background-size: cover;
`
